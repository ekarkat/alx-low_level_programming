#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * * @n: the variable
 * Return: none
 */

int _abs(int n)
{
	int b;

	if (n < 0)
	{
		b = -n;
		return (b);
	}
	else if (n == 0)
	{
		return (48);
	}
	else
	{
		return (n);

	}
}
