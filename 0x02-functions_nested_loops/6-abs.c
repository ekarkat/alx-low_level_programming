#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * * @n: the variable
 * Return: none
 */

int _abs(int n)
{
	int b;

	if (n < 0)
	{
		b = -a;
		_putchar(b);
	}
	else if (n == 0)
	{
		_putchar(48)
	}
	else
	{
		_putchar(a);
	}
}
