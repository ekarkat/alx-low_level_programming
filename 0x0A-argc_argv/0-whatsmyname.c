#include <stdio.h>
/**
 * main - print program name
 * Return: Always 0
 * @argc : argument numbers
 * @argv : arguments entered
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
	return (0);
}
